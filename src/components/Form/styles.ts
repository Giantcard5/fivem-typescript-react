import { styled } from '@stitches/react';

export const Container = styled('div', {
    variants: {
        type: {
            login: {
                width: '100%',
            },
            register: {

            },
        },
    },
});